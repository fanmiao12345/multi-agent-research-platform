# 实用化实施清单

更新日期：2026-09-09。产品方向：本机个人资料整理与研究写作助手。

本文件负责逐项状态；`PRACTICAL_RESEARCH_WRITING_PLAN.md`保存完整设计和验收门槛；
`IMPLEMENTATION_LOG.md`按完成时间追加证据。旧01～130步骤是组件实现记录，不能等同产品完成。
状态：待实施 / 实施中 / 部分完成 / 已验收（离线） / 已验收（真实业务）。
B1基线174项，B2后199项，B3后255项，B4后330项，B5后347项，S4后372项，S5后378项离线测试通过；S6新增9项运维/评测离线测试；改稿链+追问入口+专职评测Agent共400随批末全量回归确认；20个业务案例、10个故障案例尚未执行真实业务验收。

## 完整执行顺序

| 顺序 | 范围 | 交付 | 当前状态 |
|---|---|---|---|
| B1 / S0及部分S1 | 案例、模式边界、配置诊断 | 20+10案例、真实与Mock不混报 | 已验收（离线）；真实服务未验收 |
| B2 / S1 | 统一入口、根任务账本和限制 | Web/CLI/评测同一路径，辅助调用计费可查 | 已验收（离线），见B2_DELIVERY.md |
| B3 / S2 | 本地文本导入、来源和产物 | 全文、哈希、定位、去重、路径边界 | 已验收（离线），见B3_DELIVERY.md；URL/搜索属B4 |
| B4 / S2 | 网页与搜索 | 实际抓取正文、来源状态、网络安全边界 | 已验收（离线），见B4_DELIVERY.md；搜索服务需实施时选定 |
| B5 / S3 | 证据→素材→提纲→初稿→审校 | 带可核查引用的报告、草稿等级与有限修订 | 已验收（离线），见B5_DELIVERY.md；真实模型业务验收待S6 |
| S4 | 会话、取消、恢复、审批、幂等 | 重启后可继续，版本和操作状态持久化 | 部分完成，见S4_DELIVERY.md；核心库与链恢复已验收（离线），执行器/Web/Agent-loop接线随S5 |
| S5 | 完整工作界面 | 资料、正文、引用、改稿、版本、导出 | 部分完成，见S5_DELIVERY.md：研究链接入Web（表单/队列/进度/停止/恢复/证据对照/版本/导出/安全）已验收（离线）；追问改稿、审批页、大日志分页与浏览器人工验收未做 |
| S6 | 安装、真实评测和个人试用 | 60次业务运行、20次故障试验、7天试用 | 部分完成，见S6_DELIVERY.md：评测运行器/运维工具/评分表/模板已验收（离线）；真实执行需 Key 与预算、7天人力试用 |
| S7 | 按证据优化 | 有界并行、检索优化、模型路由、可选格式 | 可选，不能阻塞首版 |

试用版A=S1+本地资料+最小S3；试用版B增加联网与完整引用；候选版=S4+S5；日常1.0须S6达标。
不预设日期，不以测试数量替代业务通过率。

## 逐项检查

| 编号 | 工作 | 状态 | 证据/未完成点 |
|---|---|---|---|
| S0-01 | 准备 20 个业务任务：8 个资料整理、8 个联网/混合研究、4 个改稿；测试材料使用公开或合成数据。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-02 | 标注必需章节、关键事实、已知冲突、允许使用的来源和不能补充的内容。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-03 | 准备 10 个故障样例：无效配置、断网、空正文、冲突、预算不足、取消、重启、重复提交、路径越界、资料内指令注入。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-04 | 保存当前 148 项测试基线；按正确行为更新断言，不为保留数字而保留错误语义。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-05 | 明确 Mock/真实评测模式，修复 --no-skip 仍使用 Mock 的问题；真实模式禁止静默回退。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-06 | 检查版本管理基线及敏感文件，建立可回退版本；不能无差别提交所有 untracked 文件。 | 已验收 | 首次基线提交 b821308 + c21afbc（274文件，工作树干净）；审计排除 .env/.venv/workspaces/.tmp/node_modules/构建产物；legacy/config.ini（真实Key）忽略不提交；提交内无密钥（grep验证）；仓库级身份不写全局 |
| S1-01 | 建立统一研究任务请求与应用入口，供 Web、CLI、评测共用。 | 已验收（离线） | B2：ResearchApplication供Web/CLI/Agent Benchmark共用；资料业务在B3接入 |
| S1-02 | 显式区分 mock/real；真实模式缺配置、认证失败或模型不可用时明确失败，不能回退成假成功。 | 部分完成 | B1已有局部实现；剩余项须按完整要求验收 |
| S1-03 | 定义配置优先级：任务显式选择 > 项目模型档案 > 项目基础配置。记录实际 provider/model/能力/价格表版本，禁止档案悄悄覆盖选择。 | 部分完成 | B1已有局部实现；剩余项须按完整要求验收 |
| S1-04 | 先支持一个用户现有兼容配置，验证普通输出与工具调用。能力不单靠模型名字推断。 | 已验收 | deepseek-v4-flash 真实验证（2026-09-09）：通用任务工具往返 b01 passed；业务链 o01/o02/r01/v01 四类别 accepted；模型能力经由真实运行与记账确认 |
| S1-05 | 统一 LLM 调用入口，记录 root_job_id、角色、用途、模型、耗时、用量。覆盖 Planner、Replanner、Writer、Reviewer、Rerank、摘要等所有调用。 | 已验收（离线） | B2网关覆盖现有全部src调用点；线程与角色关联测试通过，旧独立演示不自动创建根任务 |
| S1-06 | 区分超时、连接、限流、认证、上下文超限等错误；只对合适的瞬时错误有限重试，记录重试与未知用量。 | 部分完成 | 关闭SDK隐藏重试，失败计未知用量并停止；细分错误/显式重试待实施 |
| S1-07 | 设置根任务总调用次数、输出 Token、估算费用、运行时间的初始限制，辅助调用和重试也计入。 | 已验收（离线） | B2初始四类限制通过；时间为边界检查，费用是估值，不是硬截止/账单封顶 |
| S1-08 | 页面显示实际模式、模型和可用工具；支持取消排队任务，运行中先显示取消请求，S4 完善执行边界。 | 部分完成 | 模式、模型、限额和用量页面已接；受控队列和取消仍待实施 |
| S1-09 | 检查配置 repr、异常链、HTTP 头、工具参数和导出中的敏感信息。保存必要脱敏诊断，不向前端返回真实密钥。 | 部分完成 | B1已有局部实现；剩余项须按完整要求验收 |
| S2-01 | 实现文本/TXT/Markdown 导入、全文保存和来源登记，区分成功、空内容、不支持、部分提取。 | 已验收（离线） | B3：ok/partial/duplicate/empty/unsupported/too_large/read_failed 全分类登记；原文只读；GB18030 兼容 |
| S2-02 | 实现 URL 获取与正文提取，限制连接/读取耗时、重定向、下载大小和内容类型；搜索摘要不能冒充已读正文。 | 已验收（离线） | B4：http/https抓取+正文提取（fetch_url/extract_document）；超时/10MB下载上限/类型白名单/重定向上限；正文与摘要分离。真实公网服务与复杂页面质量待S6 |
| S2-03 | 接一个配置明确的搜索服务，保存查询、排名、URL、标题与摘要；未配置时禁用搜索，保留本地/指定链接模式。 | 部分完成 | B4：未配置即禁用（SearchNotConfigured可操作提示、不假搜）；provider接入点与参数就绪；真实服务商待用户选定并按官方接口核验 |
| S2-04 | 实施时核查搜索服务官方接口、费用与限制；付费搜索也计入任务账本，无法估算时显式标记。 | 部分完成 | B4：SearchRecord 预留记账字段，未知成本显式 None 不记零；官方接口与费用核验待接入服务商时执行 |
| S2-05 | 为来源生成 id、获取时间和内容哈希，保存原始/最终 URL；发布日期未知就标 unknown，不把抓取时间冒充发布日期。 | 已验收（离线） | B3本地+B4网页：source_id/获取时间/sha256/规范化哈希；original_address=原始URL、final_url=重定向后URL；published_date=unknown 不冒充 |
| S2-06 | 按标题与段落切分，保留原文定位；去重相同内容，转载不当成多份独立证据。 | 已验收（离线） | B3本地+B4网页：提取正文带标题/段落，存meta字符偏移定位；同哈希/空白折叠同文判重（跨本地与网页），duplicate_of 指向原来源；转载判定以全文实质相同为限（近似转载/改写待真实样本评估） |
| S2-07 | 受控保存/读取完整产物，模型通过 artifact_id 或相对路径访问，不让报告只藏在日志里。 | 部分完成 | B3完成存储层：ArtifactStore版本/哈希/防覆盖，artifact_id与受控相对路径读取、孤儿与篡改拒绝；模型工具接通在B5 |
| S2-08 | 限定导入与输出范围，校验规范化路径、符号链接/Windows junction，禁止越界及覆盖原始资料。 | 已验收（离线） | B3：canonical+最长已存在前缀解析，junction/符号链接与不存在尾部逃逸均拒；索引与写路径全过resolve_under；原始资料永不回写。URL下载路径随B4复验 |
| S2-09 | 资料 URL 仅允许预期协议；拦截私网、回环、链路本地等地址，重定向重新校验，处理 DNS 解析与连接间地址变化。可信本地模型 endpoint 使用独立策略。 | 已验收（离线） | B4：仅http/https、禁userinfo；解析集整体白名单（私网/回环/链路本地/组播/保留/文档网段、IPv4-mapped按v4）；每跳重定向重新parse+resolve并直连解析出的IP；可信场景经 allowed_hosts 独立放行。真实DNS rebinding攻击样本/代理场景待S6 |
| S2-10 | 网页/文档作为不可信资料，不能改变系统指令、工具权限或授权外部访问；资料内的读取密钥等指令不得执行。 | 已验收（离线） | B4：网页文本无执行路径、原样入来源；测试固化：不改请求快照/系统指令、工具注册表不变、任务目录外无新文件。B5成稿链把资料喂给模型后再做真实语义复验 |
| S2-11 | 文本型 PDF 单独接入一个必要解析依赖，限制时间与大小并保留页码；扫描件明确提示需要 OCR。 | 待实施 | — |
| S3-01 | 实现资料整理、研究写作两条业务路径；固定阶段先通过，不由 LLM 任意生成执行架构。 | 部分完成 | B5：固定链已实现（证据→素材→提纲→初稿→双层审校→≤2轮修订），整理/写作由任务目标经同一链引导；真实业务案例验收待S6 |
| S3-02 | 修复 writer 丢失前序材料；交接 artifact_id、证据 id 和必要摘要，按需读取完整材料，列表摘要不能作为唯一业务输入。 | 部分完成 | B5：证据id/artifact_id显式交接、修订回带上一稿；引文带完整摘录与定位；模型"按需读取完整材料"的读取工具未开（当前按预算给引文+证据摘录），接入随后续批次 |
| S3-03 | Worker 明确接收前置产物；真正应用角色模型、技能、工具和权限，并与任务允许范围取交集。 | 部分完成 | B5：链上角色化（evidence/material/writer/reviewer 记账与提示）并显式传前置产物；研究链暂不开放工具（最小权限），多Worker编排沿用旧策略组件 |
| S3-04 | 证据提取输出来源、摘录、定位和支持的事实；程序先检查来源/位置存在，再由模型检查语义。 | 已验收（离线） | B5：quote逐字存在校验+段落定位后才入库，未定位即丢弃记error；evidence.json 权威存储 |
| S3-05 | 素材包按主题组织事实、证据、重复项、冲突和待补问题；不能由模型无依据地消除矛盾。 | 已验收（离线） | B5：冲突status强制open（模型"解决"被拒并记issue）；重复来源由程序登记补齐 |
| S3-06 | 提纲与正文区分事实、推断、未知；正文引用稳定证据 id，渲染为可点击的用户引用。 | 部分完成 | B5：提纲 required_evidence + 正文〔事实/推断/未知〕标注与 [E-xxx] 引用标记完成；"渲染为可点击引用"随S5工作台 |
| S3-07 | 每次调用前组装上下文，必需资料优先，按模型窗口控制长度；压缩仍保留原文引用，工具请求/响应成对保留。 | 部分完成 | B5：单来源40k/素材20k字符预算与显式截断标注、证据索引带id+摘录；"必需资料优先排序"与窗口自适应待真实模型样本后调 |
| S3-08 | 首版技能按业务模式明确配置；不依赖复杂自动召回决定权限，也不把全部技能塞进提示词。 | 部分完成 | B5：阶段固定提示词即"按业务模式配置"，research链不装技能/工具；skills 自动路由未接入（保持最小权限） |
| S3-09 | 双层审校：程序检查引用存在、格式、章节；模型检查支持关系、遗漏与矛盾。模型自评不能单独证明事实正确。 | 已验收（离线） | B5：程序层（引用/覆盖/章节/标注）+模型层（support/missing/conflict/style）；模型accepted 含error自动改判 |
| S3-10 | 建议最多修改 2 轮，每轮保存问题、新稿和变更。预算不足或仍有关键缺口时交付“待完善草稿”，不能显示验收成功。 | 已验收（离线） | B5：≤2轮修订，问题清单 review.vN、新稿 report.v(N+1) 版本化保存；预算停止/轮次耗尽交付draft |
| S3-11 | 执行结束与业务成功分开；最大轮数、引用失败和材料缺失不一律显示成功。 | 已验收（离线） | B5：accepted/draft/failed 分级映射 job 终态 completed/partial/cancelled/failed；final_text 保留草稿全文 |
| S3-12 | 保存每阶段产物与计划快照，为 S4 提供稳定恢复边界，暂不承诺任意图节点恢复。 | 已验收（离线） | B5：阶段产物全落 ArtifactStore，pipeline.json 阶段快照；真实恢复流程 S4 接通 |
| S3-13 | 给计划增加完整校验：重复 id、未知依赖、循环、空计划、失败传播；重规划后重验，受影响旧产物标记失效。 | 部分完成 | B5：链内契约校验（引用/证据id/空章节/失败传播）；链为固定顺序不生成任意图；DAG级校验在 planning 模块既有测试覆盖 |
| S4-01 | 用标准库 SQLite 保存任务/会话/阶段/审批/操作账本，提供 schema_version、迁移和备份。 | 已验收（离线） | src/harness/state/db.py：jobs/sessions/approvals/operations/meta；schema_version=1，高版本拒绝写入；在线备份+verify；损坏显式StateDbError不伪装空库 |
| S4-02 | 定义 queued、running、waiting_human、cancel_requested、completed、partial、failed、cancelled、interrupted，与底层 Run 状态显式映射。 | 已验收（离线） | states.py：9状态词+合法迁移表+底层Run映射+终态不可回写 |
| S4-03 | 有界队列、持久化领取/租约、启动扫描；禁止两个执行者同时恢复同一任务。 | 部分完成 | 队列组件已验收：原子领取、心跳、过期回收、startup_scan→interrupted、并发唯一赢家测试；常驻执行器与Web提交接线随S5 |
| S4-04 | 产物保存后才提交阶段成功；对账“产物已写、状态未提交”等崩溃窗口，避免盲目全量重跑。 | 已验收（离线） | runner顺序=产物→检查点→stage_hook外部状态；已有检查点拒绝无意识重跑；resume按文件恢复；操作账本 unknown 显式标记 |
| S4-05 | 加载本会话的目标、有效材料、当前版本及必要历史完成续写，默认不串用其他会话资料。 | 部分完成 | SessionStore+材料快照+跨会话隔离测试通过；链续写加载与版本恢复的完整接线随S5会话页 |
| S4-06 | 实现分阶段取消，停止新调用；已发送请求不能保证撤回，区分“正在停止”和“已停止”。 | 部分完成 | 队列两段取消+research链阶段边界收敛已验收（requested→cancelled/partial）；通用Agent循环取消门随S5执行器 |
| S4-07 | 可控本地解析/计算放在有界子进程中，超时可终止；外部操作依赖接口幂等能力，不宣称可安全强杀任意线程。 | 已验收（离线） | control/subprocess_guard.py：超时真实kill、输出截断；用途由接入方按需决定 |
| S4-08 | 操作账本使用 pending/running/succeeded/failed/unknown；幂等键绑定业务动作及版本。已知成功才回放，unknown 必须核实。 | 已验收（离线） | state/ops.py：op_key=job:action:version:params_hash；succeeded才回放；unknown拒绝自动重放 |
| S4-09 | 审批持久化，绑定动作、参数哈希、范围、版本和到期时间；参数改变、重规划和取消使旧审批失效，不只凭审批者字符串放行。 | 部分完成 | state/approvals.py 全部绑定与失效语义已验收；工具层审批接线（把内存 pending 换成库）随S5 |
| S4-10 | 完善根预算：各阶段、子 Run、辅助调用、重试与搜索统一计入；并行前预留、结束后结算；未知成本不能记为零。 | 已验收（离线） | B2网关+链purpose+子run/线程共享测试；未知成本显式None/阻止新调用；并行分支沿用串行化决策（未放开并行则无预留需求） |
| S4-11 | 重复/无进展检测依据结构化调用与产物变化，不能靠回答中的关键词决定是否成功。 | 部分完成 | control/progress.py 按(llm调用数,产物哈希)判定已验收；执行器看门狗接线随S5 |
| S4-12 | 先按业务阶段恢复；确需节点级恢复再接官方持久化 Checkpointer，避免出现两个独立调度系统。 | 已验收（离线） | 阶段检查点+resume_research_job+CLI --resume-job；真实子进程os._exit崩溃→续跑完成且不重复抽取；节点级官方checkpointer未接（决策：阶段边界足够） |
| S4-13 | 旧运行可读取；迁移失败明确提示，不以忽略解析错误、返回空列表伪装成没有历史。 | 已验收（离线） | 旧run/plan.json 布局不变（B2/B3全量回归证明）；库损坏/高版本/快照损坏全部显式报错 |
| S5-01 | 表单提供目标、资料、模式、读者、长度、模型、预算；高级参数折叠，常用默认值清楚。 | 部分完成 | 目标/资料(文件/链接/粘贴)/模式/流程/预算/次数Token秒数在表单项；读者与长度（成稿风格）与高级参数折叠未做 |
| S5-02 | 任务列表、进度、排队/停止/恢复、资料列表、最终报告成为基本视图；日志放详情。 | 已验收（离线） | 研究任务经SQLite队列（submit→claim→run→release）；㉑卡片任务列表/阶段进度/停止/恢复；agent run 走原Dashboard；日志/分级在详情区 |
| S5-03 | 正文与来源可对照；点引用能看到原文片段和位置，不只是网站首页。 | 已验收（离线） | 报告按[E-编号]分词渲染成按钮，点击展示 evidence 摘录/定位/来源；来源全文仍可点开 |
| S5-04 | 支持追问改稿、版本列表和主要变更，修订不覆盖旧版本。 | 部分完成 | 版本列表/查看/导出、单次改稿（base_draft+no_change 守卫）与追问改稿（CLI --revise-job / Web revise 端点，谱系 revises_job，旧版永不覆盖）已做；"主要变更"自动说明与会话表记忆随试用反馈 |
| S5-05 | Markdown/HTML 下载只允许任务内产物；预览与导出都处理脚本、事件属性、危险 URL 和原始 HTML。 | 已验收（离线） | 导出仅登记产物（attachment+nosniff）；预览/渲染全走 textContent/DOM 不执行；HTML富文本预览未做（text/plain pre） |
| S5-06 | 审批页说明动作、参数、范围和失效原因；已授权的常规资料读取不反复弹窗。 | 部分完成 | HITL 面板展示动作/参数；持久化审批库与失效语义已验收（S4）；失效原因展示与常规读取授权记忆页面未做 |
| S5-07 | 错误能指导操作，例如“PDF 无可提取文本”“搜索未配置”“模型不支持工具”，不只显示异常类型。 | 已验收（离线） | 导入分类/搜索未配置/链结果分级消息贯穿 HTTP400/页面；错误同时保留类型+message |
| S5-08 | 先完善轮询游标、断线重连、部分日志行、大日志分页；只有体验需要时再加 SSE/Token 流。 | 部分完成 | after 游标/断线重连（showJob 循环容错）/增量事件已有；大日志分页未做、SSE 未加（符合"必要再加"） |
| S5-09 | 写接口校验 Host/Origin、请求类型/大小和会话防护，默认只绑定回环地址。 | 已验收（离线） | Host回环白名单403/请求体≤1MB 413/仅 application/json 415；服务默认绑定127.0.0.1；会话防护沿用 approval_id/job_id 绑定 |
| S5-10 | 清晰呈现无 Key、无搜索、预算不足和部分完成；页面只暴露实际可用能力。 | 部分完成 | mode/config ready 明确、导入错误与预算/取消分级消息呈现；搜索配置状态入口与"页面只暴露可用能力"进一步收口未做 |
| S6-01 | 评测复用业务入口，不能另写更简单的测试流程；报告记录代码/数据集版本、实际模型、工具和配置快照。 | 已验收（离线） | eval/business_eval.py 全走 ResearchApplication(flow=research)；报告含 dataset version/代码版本/Python与包版本/模型与工具/脱敏配置快照 |
| S6-02 | 固定资料与 HTTP 样本用于离线回归，实际服务用于联网冒烟；两类结果分别报告。 | 部分完成 | 固定合成资料离线回归已通（stub 2/2、Mock 诚实失败）；实际服务联网冒烟脚本未执行（待Key与预算） |
| S6-03 | 真实评测显式启用并设置批次限额；没有配置/额度就标未执行，不拿 Mock 成绩顶替。 | 已验收（离线） | --mode real 必须 --max-cost；缺Key整批 not_executed 且零模型请求（测试验证）；探针不发请求 |
| S6-04 | 20 个业务任务每个重复 3 次，共 60 次，保存所有失败样本；10 个故障任务至少执行两轮。 | 部分完成 | 运行器支持 repeats=3/fault_rounds=2、失败样本整目录保存；真实四类别代表案例 o01/o02/r01/v01 各1次 accepted（2026-09-09，估算约$0.23 保守价）；完整 20×3+10×2 真实批次待执行（每次约2~10分钟/案例，成本以账单为准） |
| S6-05 | 明确人工评分表，模型可以辅助找问题，但不能自评分后自动盖章。 | 部分完成 | eval/human_scores.py 人工评分表 + eval/grader.py 专职评测 Agent（独立模型按评分表打分、程序交叉核验引用与伪造、computed_verdict 规则重算、human_confirmed=false 不自动盖章，真实演示 o02/v01 均 5/5/5/5→accept）；人工确认/覆盖入口试用期补充 |
| S6-06 | 记录全任务与阶段耗时、已知/估算费用、失败原因、人工改稿时间；与相同任务的单 Agent 或手工基线比较。 | 部分完成 | 逐次记录耗时/费用/未知用量/失败原因/终止原因/机器检查；改稿分钟入表；单Agent/手工基线对比未做 |
| S6-07 | 锁定已验证依赖和 Python 版本，修正安装/包发现配置；全新虚拟环境验证安装、启动、测试和样例。 | 部分完成 | requirements.lock.txt（Python 3.14.7 + 五依赖精确版本）+ python -m src.ops.verify（导入/CLI样例/诊断/健康）；真正全新venv执行留待人工 |
| S6-08 | 提供项目内启动脚本、健康检查、配置诊断、日志位置、备份恢复说明，不动全局环境和 DSH。 | 已验收（离线） | src/ops/{health,backup,verify}.py + 启动/诊断命令文档；备份=sqlite在线备份+jobs/sessions复制+manifest（roundtrip测试）；恢复说明防覆盖运行区 |
| S6-09 | 必要的浏览器回归工具作为开发依赖，覆盖正文、引用、审批、取消和改稿，不混入生产依赖。 | 部分完成 | docs/BROWSER_REGRESSION.md：覆盖矩阵+引入步骤（HTTP级已有等价覆盖）；工具引入待需要时，人工验收随试用 |
| S6-10 | 进行 7 天个人试用，至少记录 20 个真实使用任务；问题转成可复现用例，再判断是否发布 1.0。 | 待实施 | docs/TRIAL_LOG_TEMPLATE.md 模板与自查/判定步骤已备；执行需真实 Key 与 7 天人力 |

## 本批B2拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| P00 | 核对完整计划，建立逐项清单与追加日志 | 已完成 |
| B2-01 | 统一请求、根任务目录与应用执行入口 | 已验收（离线） |
| B2-02 | 模型调用网关、持久化调用账本、次数/输出/时间/费用限制 | 已验收（离线） |
| B2-03 | 主循环与Planner/Replanner/Judge/Rerank/摘要接网关；线程上下文传递 | 已验收（离线） |
| B2-04 | Web、CLI、评测共用应用入口；可查看根任务用量 | 已验收（离线） |
| B2-05 | 集成回归、失败路径、浏览器验证和交付记录 | 已验收（离线） |

## 本批B3拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| B3-00 | B2回归复核；修复 /job 轮询偶发 AttributeError（run.json root_job_id 写入窗口） | 已完成 |
| B3-01 | 受控路径边界：canonical/最长已存在前缀解析、junction与不存在尾部逃逸拒绝（S2-08本地） | 已验收（离线） |
| B3-02 | 来源登记与导入：文本/TXT/Markdown分类、全文/哈希/meta、标题段落定位、同文去重（S2-01/05/06本地） | 已验收（离线） |
| B3-03 | 受控产物 ArtifactStore：版本/哈希/防覆盖、artifact_id与相对路径受控读取（S2-07存储层） | 已验收（离线） |
| B3-04 | TaskRequest 携带资料字段；ResearchApplication/CLI 导入接入与导入摘要（统一入口原则） | 已验收（离线） |
| B3-05 | Web /api/jobs 资料与产物只读端点、页面⑳面板、全量回归与交付记录 | 已验收（离线） |

## 本批B4拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| B4-01 | URL策略与SSRF/私网防护：协议/格式白名单、地址网段拦截、DNS解析集整体校验（S2-09离线） | 已验收（离线） |
| B4-02 | 抓取器与正文提取：http/https直连白名单IP、重定向逐跳复检、超时/大小/类型限制；HTMLParser正文（S2-02） | 已验收（离线） |
| B4-03 | SourceStore 登记 URL 来源：kind=url、原始/最终URL、http状态、内容类型、跨来源转载去重（S2-05/06 URL侧） | 已验收（离线） |
| B4-04 | TaskRequest.urls/allow_network + 统一导入入口 import_request_sources + CLI/Web 接入（S2-09应用侧） | 已验收（离线） |
| B4-05 | 搜索占位网关（S2-03/04：未配置即禁用、账本字段、可操作提示）+ S2-10 惰性测试证据 | 已验收（离线） |
| B4-06 | 全量回归与交付记录（B4_DELIVERY.md），同步状态文档 | 已完成 |

## 本批B5拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| B5-01 | 数据契约与证据层：Evidence/MaterialPack/Outline/Review 契约、evidence.json、引用标记与稳定编号（S3-04数据面） | 已验收（离线） |
| B5-02 | 提示词模板与上下文预算：40k/20k字符上限、截断显式标注、证据索引带id+摘录（S3-07雏形） | 已验收（离线） |
| B5-03 | 证据提取与素材包阶段：quote逐字定位、冲突强制open、重复来源程序补齐（S3-04/05） | 已验收（离线） |
| B5-04 | 提纲与初稿阶段：required_evidence校验、[E-xxx]引用与〔事实/推断/未知〕标注（S3-06） | 已验收（离线） |
| B5-05 | 双层审校与≤2轮修订：程序+模型审校、review.vN/report.v(N+1)版本化（S3-09/10） | 已验收（离线） |
| B5-06 | runner：固定阶段、pipeline.json快照、accepted/draft/failed分级、预算停止收敛（S3-11/12） | 已验收（离线） |
| B5-07 | 入口接入：TaskRequest.flow、ResearchApplication同账本跑链、CLI --flow research、Web守卫（S1-01延续） | 已验收（离线） |
| B5-08 | 全量回归与交付记录（B5_DELIVERY.md），同步状态文档 | 已完成 |

## 本批S4拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S4核心 | state/{db,states,queue,ops,approvals,sessions}.py：SQLite库/状态词与迁移表/队列租约与两段取消/操作账本/持久化审批/会话隔离（S4-01/02/03组件/06队列侧/08/09库侧/13） | 已验收（离线） |
| S4链层 | runner 检查点与 resume、should_stop 边界收敛、stage_hook 先文件后状态、崩溃窗口恢复（S4-04/06链侧/12） | 已验收（离线） |
| S4能力 | control/subprocess_guard.py（有界子进程）与 progress.py（无进展检测）（S4-07/11组件） | 已验收（离线） |
| S4入口 | resume_research_job 应用入口 + 续接账本（原调用载入、上限延续）+ CLI --resume-job（S4-10/12应用侧） | 已验收（离线） |
| S4接线 | 常驻队列执行器、Web 提交/取消/审批/会话页面、Agent循环取消门（S4-03/05/06/09/11 剩余接线） | 待实施（随S5） |

每步完成立即追加日志，至少包含：日期、对应编号、修改文件、验证命令/结果、限制与下一步。
设计与代码存在但尚未验证时只能标“实施中”，不能提前写“已验收”。

## 本批S5拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S5-01 | ResearchApplication.run 注入 job_id/progress/stage_hook/should_stop | 已验收（离线） |
| S5-02 | Web×S4接线：state.sqlite+JobQueue+worker（submit→claim→run→release 分级收敛）与㉑任务卡 | 已验收（离线） |
| S5-03 | 端点与视图：/api/jobs、progress、evidence、artifacts download、cancel、resume | 已验收（离线） |
| S5-04 | 页面交互：表单flow/任务跟踪/停止/恢复/报告分词引用/版本/导出 | 已验收（离线） |
| S5-05 | 写接口安全（Host/大小/类型）与安全渲染（DOM/textContent、attachment+nosniff） | 已验收（离线） |
| S5-06 | 全量回归、交付记录与文档同步 | 已完成 |
| S5-剩余 | 追问改稿（会话式）、审批失效原因页、读者/长度与高级折叠、大日志分页、HTML富预览、搜索能力展示收口、真实浏览器人工验收 | 待实施（部分随S6/后续批次） |

## 本批S6拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S6-运行器 | eval/business_eval.py：统一入口评测、real缺Key→not_executed、repeats/fault_rounds、失败样本保存、逐次指标、版本/配置快照（S6-01/03/04/06） | 已验收（离线） |
| S6-评分表 | eval/human_scores.py 逐次人工评分表（机器检查列明示仅供参考，不自评分盖章）（S6-05） | 已验收（离线） |
| S6-运维 | src/ops/{health,backup,verify}.py + requirements.lock.txt（S6-07/08） | 已验收（离线） |
| S6-模板 | docs/TRIAL_LOG_TEMPLATE.md、docs/BROWSER_REGRESSION.md（S6-09/10） | 已完成（模板级） |
| S6-真实执行 | 真实模型60次/联网冒烟/故障两轮/人工评分导入/7天试用/单Agent基线对比/全新venv安装验证 | 待实施（需Key+预算+人力） |




| S6-评测Agent | eval/grader.py 专职评测：评分表打分+程序核验+规则重算+独立账本+human_confirmed=false | 已验收（离线+真实演示） |
| S6-人工 | 人类评分/确认与覆盖入口、20案例全量评分执行 | 待实施（试用期/用户） |
