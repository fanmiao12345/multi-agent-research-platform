# 完整项目实施清单

更新：2026-09-11。主计划：[PROJECT_MASTER_PLAN.md](PROJECT_MASTER_PLAN.md)。整体目标是个人 AI 智能体系统，研究写作是首个完整场景。

## 当前执行规则

先 D0～D10 完成全部功能，再冻结并进入 Q1～Q4 整体测试与优化。D 阶段只做最小功能/关键边界验证与阻塞修复，不反复全量回归、真实批次或质量调参。

下一步：用户自行执行 **Q2-01**；工具与人工评分工作台已就绪，真实费用、人工评分和 7 天试用不由 Codex 代跑。

状态：待实施 / 部分实现 / 实施中 / 功能完成 / 外部条件阻塞 / 整体通过。下面“部分实现”表示已有基础或独立组件，尚未完成本计划要求的统一接入；不能视为已验收。

D0-01～10 已完成功能开发与冻结清点；Q1-01 候选版全量离线回归已通过，当前进入 Q1-02。

## D 阶段：48 个功能开发步骤

| 步骤 | 实施内容（具体完成证据见总计划） | 当前状态 |
|---|---|---|
| D0-01 | 写清整体目标、完整范围、用户流程与功能/优化边界 | 功能完成（文档） |
| D0-02 | 统一主计划、场景计划、编排设计、旧计划的优先关系 | 功能完成（文档） |
| D0-03 | 将现有组件与缺口映射新步骤，建立记录和待优化清单 | 功能完成（文档） |
| D1-01 | 统一任务请求：目标、工作类型、文本/文件/URL、会话/原稿、网络策略、方式、模型、Token、时间/费用与硬要求 （2026-09-11：TaskRequest 增加 orchestration 字段并入快照；执行器改为从原始请求 replace 派生子请求——files/profile/max_output_tokens/allow_network/硬要求不再丢失，显式 max_cost=0 保留为 0 不被当成不限；CLI 编排路径传完整请求。目标性验证：22 项相关测试 + plan-only/端到端冒烟通过） | 功能完成（最小验证） |
| D1-02 | 调度前创建 root_job；子任务只有 child_id/parent_id，不新开独立预算根 （2026-09-11：CLI 调度前预留根目录；根/子 job.json 记 parent_job_id+budget_root；orchestration.json 随执行推进；最终交付运行复用根 job_id，编排信息并入根 job.json。目标性验证 23 项 + CLI 端到端冒烟。限制：子任务账本仍各自记账，网关级统一在 D2-01/02） | 功能完成（最小验证） |
| D1-03 | 定义版本化计划、任务状态、SourceRef/EvidenceRef/ArtifactRef 和结构化子结果 （2026-09-11：refs.py 定义三类引用与 StructuredSubResult；执行器为每个子任务收集证据/产物/来源引用与截断说明，入 orchestration.json；计划已带 schema_version。目标性验证通过。引用追溯原始资料在 D7-01 接通） | 功能完成（最小验证） |
| D1-04 | 定义统一返回、执行状态与交付等级，旧任务读取提供版本兼容 （2026-09-11：contracts.py 定义交付等级/执行状态两套词汇与归一化规则（unable=诚实拒绝≠完成；未知值保守落 failed/partial），unified_record 统一收尾形状；执行器记录附 unified 块。旧任务读取兼容：resume 与字段 .get 容错已有；目标性验证 48 项通过） | 功能完成（最小验证） |
| D2-01 | 调度、规划、压缩、研究、审校、重规划、工具与搜索统一经过根网关；记录用途、角色、父子关系、耗时与未知用量 （2026-09-11：调度调用经 model_call 入根账本（purpose=orchestration_plan/role=scheduler）；JobLedger 续接——同一 job 的调度/链/汇总条目共存一份 ledger.json；child_run 汇总入根账本（费用计入根预算、不占调用次数）；失败子运行保留 child_job_id 可追溯。目标性验证 60 项 + CLI fixed/fanout 冒烟。搜索记账随 D3-01；压缩/重规划调用随 D4/D5 接入同一机制） | 功能完成（最小验证） |
| D2-02 | 发请求前原子预留，完成后结算/释放；包含子任务、重试、在途与成稿预留；未知结果显式保留 （2026-09-11：JobLedger.reserve/settle/release 协议——预留写入 ledger.json（跨句柄写前重读），余量不足即 BudgetStop；未知费用按预留额保守入账并标记 conservative；零预算零请求（成稿运行也不启动）；执行器 fanout 每个子任务先预留后结算，预留失败显式记失败。目标性验证 63 项 + CLI 冒烟。锁粒度仍为单账本串行，真并行在 D6-04） | 功能完成（最小验证） |
| D2-03 | 角色模型/技能/工具/权限真正生效；角色与子任务权限取用户授权和父级权限的交集 （2026-09-11：TaskRequest 新增 allowed_tools（None=不限）；执行器按角色档案取"角色声明 ∩ 父级授权"交集并落子请求，生效配置（工具/技能/模型/覆盖值）入编排记录；补 editor 角色对齐契约白名单；工厂已保证真实模式失败不切 Mock。目标性验证 65 项。技能召回接入在 D4-03） | 功能完成（最小验证） |
| D2-04 | 统一工具执行、结果截断/完整产物、超时/可重试分类；将已配置 MCP Client 工具纳入同一机制 （2026-09-11：既有 ToolExecutor 链（schema→权限→风险→重试分类→超时→截断）复核完整；新增 MCP_SERVERS 配置驱动接入（bootstrap.py：解析/启动/握手/发现注册/生命周期 close，失败显式报错不静默降级）；未知外部操作默认 side_effect=True 不盲目重试，只读工具可配置放开；研究应用自动接入已配置 MCP Server 并随 run 结束关闭。目标性验证 74 项（含 9 项 MCP）） | 功能完成（最小验证） |
| D3-01 | 接搜索服务：项目配置、请求/响应映射、认证/限流/超时/空结果、搜索根账本 （**2026-09-11 用户指令变更：爬虫先行**——bing_scrape 已实现：现有 fetcher 直抓 cn.bing.com 结果页 + 标准库解析 b_algo（标题锚覆盖面包屑锚），被拦/无结果/改版显式 SearchError 不假装；provider=bing_scrape 仅真实模式可用，调用以 kind=search 入根账本（不计模型调用次数）。真实联调一次通过（5 条真实候选，干净标题+URL）。**百度官方 API 保留为后续稳定化选项**，待用户申请 Key 后接入） | 功能完成（最小验证） |
| D3-02 | 生成有界查询、站点/时效筛选、候选列表与初始选择规则；不开网络时不搜索 （2026-09-11：plan_queries 支持有界 2~4 查询、site: 与 after: 过滤；自动搜索结果保存 provider/query/rank/title/URL/snippet/published_date/date_source；URL 去片段去重，逐查询读取 Settings.search_max_results 并按 max_candidates 截断。限制：site/since 目前由调用方传入，CLI/Web 暂未单独暴露参数） | 功能完成（最小验证） |
| D3-03 | 候选→真实正文→来源，继承 URL 安全策略、大小限制及 URL/正文去重 （2026-09-11：auto_search_candidates 输出候选元数据，research 入口把候选 URL 接入既有 import_request_sources；正文由 fetcher/extractor 提取，失败登记为 read_failed，同文按内容哈希标记 duplicate；搜索摘要只留候选记录，不进来源正文。定向测试覆盖候选去重/限额/单查询失败继续/片段 URL/摘要与正文分离/同文去重） | 功能完成（最小验证） |
| D3-04 | 接文本型 PDF，限制解析资源，保留页码；扫描 PDF 明确提示不支持 OCR （2026-09-11：新增 pypdf==6.18.0 与 pdf_extract；单文件 2MB、最多 200 页、解析最长 30 秒；页文本进入 SourceStore，段落 locator 带 page；空白/扫描件 unsupported 并提示 OCR，损坏/加密/超页显式分类） | 功能完成（最小验证） |
| D3-05 | 根任务共享来源库；记录获取时间、来源版本、撤回/过期与下游引用关联 （2026-09-11：SourceStore 增加 source_version/retrieved_at/root_source_id/withdraw/expire_due/add_version/find_dependents；fanout 在根任务只导入/搜索一次 shared_sources，子任务与成稿传正文副本并回链 root_source_id；SourceRef 保留根来源和版本） | 功能完成（最小验证） |
| D4-01 | 每次调用通过上下文组装器，包含当前目标、约束、任务输入和必要证据，限制窗口并显式标记省略 （2026-09-11：AgentRuntime 每次 agent 调用经 compose_context，model_gateway 对普通非工具调用统一组装；context.json 记录 skill/memory/knowledge 与每轮 token 统计；trim_messages 保持 tool request/response 配对） | 功能完成（最小验证） |
| D4-02 | Handoff 与共享成果区：按角色传 ArtifactRef/EvidenceRef，可按授权读取全文；局部上下文隔离 （2026-09-11：RuntimeContext 支持显式 handoff_text 进入 Context Builder；fanout 子任务只接收根共享来源正文和自身任务，不读取无关会话；SourceRef 回链根来源，结构化子成果保留 evidence/artifact/source refs） | 功能完成（最小验证） |
| D4-03 | 接技能召回与角色配置，记录本次技能及选择依据；技能不能提升权限 （2026-09-11：SkillRegistry/recall/router 已接入 AgentRuntime；除 deep-dive/quick-math 外新增 source-audit/literature-review/decision-brief/data-check/risk-review/project-retrospective；权限仍取入口权限∩技能 allowed_tools，选择理由写 context.json） | 功能完成（最小验证） |
| D4-04 | 接会话短期记忆、项目知识检索和用户显式保存的长期偏好；提供查看/删除及来源记录 （2026-09-11：thread_id+MemorySaver 保持跨调用短期历史；显式“记住/以后”经写入策略保存 LongTermStore；memory/knowledge 按相关性注入；list/get/forget 保留来源并支持删除；memory_enabled=false 时不写入不召回） | 功能完成（最小验证） |
| D5-01 | 理解工作类型、目标/交付要求、资料缺口与关键条件；可默认偏好不反复询问 （2026-09-11：新增 TaskUnderstanding/understand_task，区分 simple_tool/simple_qa/collection/research_report/research/revision；提取必需章节、禁语、关键事实和字数偏好；缺对象/动作时写 input_request.json 进入 waiting_input，可恢复补充；缺资料只记录 material_gaps，不反复询问） | 功能完成（最小验证） |
| D5-02 | 完整能力目录与自动选型，按可用工具/模型/网络/预算过滤；计划记录理由与预计开销 （2026-09-11：CapabilityCatalog 列出 six 模式及 implemented/tools/network/budget 条件；D5 只开放 fixed/fanout；CLI 按目录过滤，显式不可用模式只能降级 fixed、不扩大权限；plan_meta 记录 available/rejected/reason） | 功能完成（最小验证） |
| D5-03 | 程序校验任务 ID、依赖、循环、章节覆盖、预算、并发和角色；按真实依赖及成果就绪派工 （2026-09-11：ExecutionPlan 增加依赖环路 DFS 校验；fanout 先拓扑排序，前置依赖未完成/失败的任务不执行并显式记失败；原有角色/章节/预算/并发校验保留） | 功能完成（最小验证） |
| D5-04 | 重规划保留版本、已完成产物和失效关系；重试/重规划次数有限，同题/无进展检测 （2026-09-11：Plan 增加 version/parent_version/invalidated_task_ids；apply_delta 修改上游时使下游旧成果失效并重新排队，未修改的完成任务保留；执行器对重复计划指纹标记 no_progress，现有 max_replans/max_attempts 限制不变） | 功能完成（最小验证） |
| D6-01 | single：通用工具任务与简单资料处理进入统一根任务；按工作类型选择交付检查 （2026-09-11：能力目录开放 single；简单工具/问答走单根任务，不创建子运行；CLI 显式 single 进入统一编排入口） | 功能完成（最小验证） |
| D6-02 | fixed：现有研究链作为共享策略，完整沿用输入、角色配置、产物与预算 （2026-09-11：固定链继续复用 ResearchApplication，能力过滤和 CLI 统一下发；相同请求保留输入/角色/产物/预算契约） | 功能完成（最小验证） |
| D6-03 | manager_worker：统筹者按依赖派工、检查成果、继续执行；每项明确输入与输出 （2026-09-11：启发式计划生成 researcher→organizer→writer 依赖链；执行器按拓扑顺序派研究/组织角色，把上游 final_text 作为前置产出交给下游，writer 仍由根研究链完成） | 功能完成（最小验证） |
| D6-04 | fanout：在 D2 预留机制下真实有界并发，汇总成功与失败分支 （2026-09-11：执行器按依赖波次组织 ready 任务，ThreadPoolExecutor 受 plan.max_parallel 限制；波次启动前逐个 reserve，完成后 settle；成功/失败均保留结构化子结果和来源回链；并发测试确认受控慢响应实际重叠） | 功能完成（最小验证） |
| D6-05 | dynamic_team：根据中途发现的缺口调整任务/角色，受重规划与派生上限限制 （2026-09-11：能力目录开放 dynamic_team；执行器复用 plan_task/replan 动态差异，在根 job_scope 下运行规划/重规划，子任务仍按波次并发且每任务先预留后结算；每次角色运行形成结构化子结果并回链共享来源） | 功能完成（最小验证） |
| D6-06 | debate：各方基于资料产出观点/证据/反驳，由审查环节形成支持与争议清单 （2026-09-11：能力目录开放 debate；执行器并发运行支持/反对方子任务，双方各自预留结算并保留结构化引用；judge 调用在根账本内汇总支持点/争议点/证据缺口，根成稿再走研究链） | 功能完成（最小验证） |
| D6-07 | 子智能体作为受控工具，允许二层嵌套；复用预算、深度、总数、去重和权限规则 （2026-09-11：delegate_subagent 增加 ContextVar 深度/数量/同题去重护栏；子运行继承当前有效工具权限，最多二层；Agent 流自动注册受控工具，超限返回明确拒绝） | 功能完成（最小验证） |
| D6-08 | 六方式统一注册与切换/降级、共享结构化成果与生成审查机制 （2026-09-11：registry.py 注册 single/fixed/manager_worker/fanout/dynamic_team/debate；执行器按注册表分派；六模式共享根账本、来源库、结构化子结果和最终研究链；CLI options 同步） | 功能完成（最小验证） |
| D7-01 | 所有模式交接事实/推断/未知、摘录、原来源位置；汇总只接受可核验的证据引用 （2026-09-11：EvidenceRef 增加 source_job_id/root_source_id/version/locator；build_root_lineage 按摘录把根报告证据映射回子证据和原始来源；citation_lineage.json 随根任务落盘） | 功能完成（最小验证） |
| D7-02 | 按任务生成整理结果/分析/报告；复用素材、提纲、写作和版本功能，保持章节和事实约束 （2026-09-11：TaskRequest/PipelineResult 增加 delivery_kind；collection 在素材包后直接交付 collection 产物；analysis 走精简章节和程序校验；report 保持原固定研究链；显式硬章节优先 report） | 功能完成（最小验证） |
| D7-03 | 运行中检查覆盖、引用、支持关系与冲突；根据明确缺口有限补搜/补派/修订 （2026-09-11：run_research_pipeline 增加一次有界 repair_callback；明确 material gaps 触发补做，新增来源重新抽取证据并更新素材版本；联网模式由应用层执行一次有界搜索/抓取补源；无新增证据/异常如实记录） | 功能完成（最小验证） |
| D7-04 | 改稿、追加问题、来源撤回与更新；只重做受影响成果，记录变更 （2026-09-11：新增 revise_with_source_update——撤回来源后只用剩余可用资料和最新原稿创建新 job；旧稿/旧来源版本保留；source_update.json 记录撤回、依赖和剩余来源，新稿带 revises_job 谱系） | 功能完成（最小验证） |
| D8-01 | 把根/子任务、计划版本、会话与阶段接入持久状态和受控队列 （2026-09-11：state schema v2 增加 parent_job_id/plan_version/stage_history；JobQueue 持久化阶段历史并支持更新；执行器可接 state_queue，自动登记根任务和子任务父子关系） | 功能完成（最小验证） |
| D8-02 | 根取消传播到所有分支，停止新调用，区分已请求停止与已停止 （2026-09-11：执行器支持 should_stop/队列 cancel_requested；fanout 在波次间检查，取消后不启动后续分支，保留已记录子结果并收敛 cancelled；队列仍以 cancel_requested→cancelled 两段语义呈现） | 功能完成（最小验证） |
| D8-03 | 按已提交的来源/子结果/阶段恢复；外部操作结果未知单独列出，不静默重放 （2026-09-11：新增 inspect_resume_state/resume_notes.json，审计可用来源、stage_* 检查点、可复用子结果、历史调用与 unknown operations；resume 继续载入旧 ledger calls 和 reservations，未知费用不清零） | 功能完成（最小验证） |
| D8-04 | 待输入与审批持久化，绑定目标/参数/预算/版本/到期；拒绝或变更后失效 （2026-09-11：state schema v3 增加 pending_inputs；PendingInputStore 绑定 questions/target/params_hash/budget/plan_version/expiry，支持 answer/invalidate；Web waiting_input→回答→重新入队；现有 ApprovalStore 保留参数、scope、版本、到期和失效语义） | 功能完成（最小验证） |
| D9-01 | CLI/Web 同一提交入口；默认 auto，资料/网络/模型/预算等参数完整透传；只看计划不执行 （2026-09-11：CLI/Web 都经 TaskRequest 契约；Web 增加 plan_only 返回计划不入队；研究请求统一由队列执行并保留参数快照） | 功能完成（最小验证） |
| D9-02 | 首页围绕目标输入与结果；方式告知、业务进度、默认收起的计划/子任务/调用详情 （2026-09-11：任务状态显示实际方式、阶段、取消与待输入；结果区展示交付等级、硬约束、审校阶段和报告/分析/整理产物） | 功能完成（最小验证） |
| D9-03 | 来源/证据点击定位、版本、会话追问、Markdown/HTML 导出、过程记录下载 （2026-09-11：保留来源/证据/报告版本/追问；新增 HTML 导出和过程记录下载端点及页面链接；Markdown 下载继续使用受控产物接口） | 功能完成（最小验证） |
| D9-04 | 配置不可用、缺资料、超预算、等待输入/审批、停止恢复等状态统一展示 （2026-09-11：waiting_input/待审批/取消/恢复/错误从队列与 job_file 统一读取；待输入可直接回答并重新排队；状态页只显示业务消息，不返回敏感配置） | 功能完成（最小验证） |
| D10-01 | 安装/启动/配置诊断/备份恢复脚本，模型与搜索的项目内配置，依赖与数据迁移说明 （2026-09-11：scripts/install/start/backup/restore、ops.restore、INSTALL_AND_RECOVERY.md；脚本只操作项目目录，恢复需显式确认，凭据不入样例） | 功能完成（最小验证） |
| D10-02 | 本地 MCP Server 的已授权能力接线、Client/Server 使用说明和生命周期 （2026-09-11：本地只读工具、配置驱动注册、allowlist/风险/生命周期与统一执行链；MCP_USAGE.md；UTF-8 子进程协议修复） | 功能完成（最小验证） |
| D10-03 | 补齐整体测试与评测工具：同一入口、版本快照、闭卷数据隔离、账本和人工评分格式 （2026-09-11：freeze_manifest 生成源码/依赖/数据集哈希；q1_regression 全量离线报告；闭卷与人工评分格式沿用 business_eval/human_scores） | 功能完成（最小验证） |
| D10-04 | 核对 G01～G15 与 D 步骤，清点依赖/未实现项，生成候选版本与功能冻结清单 （2026-09-11：FEATURE_FREEZE.md 对照 G01～G15；freeze_manifest freeze_check=true，151 个源码/配置文件及依赖、数据集版本完成哈希冻结） | 功能完成（最小验证） |

## Q 阶段：12 个整体测试与优化步骤

前置：全部 D 功能完成并通过冻结清点。当前均未进入新候选版的整体执行；历史批次不能替代本阶段。

| 步骤 | 内容 | 当前状态 |
|---|---|---|
| Q1-01 | 候选版全量离线回归、统一入口集成、各模式/嵌套/权限/预算/长资料与参数一致性 | 整体通过（506 passed；报告 eval/reports/q1_offline_regression.json 与 .md） |
| Q1-02 | 10 类既有故障至少各两轮，并补并发预留、子任务取消、恢复未知操作、审批过期等模式故障 | 整体通过（14 类×2=28/28；报告 eval/reports/q1_fault_matrix.json） |
| Q1-03 | 真实浏览器完整用户旅程、刷新/重启、导出、MCP、全新环境安装及备份恢复 | 整体通过（真实浏览器 agent+真实研究任务；刷新/重启/导出/verify/备份恢复/MCP；报告 eval/reports/q1_browser_install.json/.md） |
| Q2-01 | 冻结完整案例集与评分：保留现有 v2 33 例，每例 3 次共 99 次；v1 20 例的 60 次结果作为子集单列 | 待用户执行（scripts/q2_real.ps1、人工评分工作台与 ingest 工具就绪） |
| Q2-02 | 补至少 10 个真实联网主题，实际搜索与正文；六种方式各至少 2 个适用案例、嵌套至少 2 个；允许与业务案例重合但须标覆盖 | 待用户执行（12 主题 web_topics_v1、六模式 web_baseline 脚本就绪） |
| Q2-03 | 汇总质量、全文引用支持、总费用含辅助调用、未知成本、端到端时延、人工修改时间 | 待用户执行（q2_summary 汇总工具就绪） |
| Q3-01 | 优先处理错误引用、来源混淆、缺口误判、选型错误；再优化检索/证据筛选/提示词 | 待用户执行（基线/候选同条件对比工具与流程就绪） |
| Q3-02 | 调整查询轮数、上下文、并发、模型路由与预算估值；同等任务比较适用模式 | 待用户执行（Q3-02 参数对比运行手册就绪） |
| Q3-03 | 完整回归与真实复验；候选能力仍保留，收益影响默认选型而不倒过来取消已承诺开发范围 | 待用户执行（Q3 对比后运行完整回归） |
| Q4-01 | 连续 7 天个人试用，至少 20 个真实工作任务，记录额外干预、改稿时间和未完成原因 | 待用户执行（trial_log 工具就绪，不能由自动批次代替） |
| Q4-02 | 集中修复试用问题，复验受影响路径，完成默认配置/操作说明/已知限制 | 待用户试用后执行 |
| Q4-03 | 对照全部目标和整体标准签收；记录版本、功能范围、验证证据、残留问题 | 待用户签收（q4_signoff 草稿工具就绪） |

## 历史实施记录（2026-09-11 重整前）

以下原清单原样保留历史语义，含当时的完成/待办/下一步；其排期已失效，旧编号按总计划第 10 节映射，不覆盖上面的当前状态。

### 旧实用化实施清单

更新日期：2026-09-09。产品方向：本机个人资料整理与研究写作助手。

本文件负责逐项状态；`PRACTICAL_RESEARCH_WRITING_PLAN.md`保存完整设计和验收门槛；
`IMPLEMENTATION_LOG.md`按完成时间追加证据。旧01～130步骤是组件实现记录，不能等同产品完成。
状态：待实施 / 实施中 / 部分完成 / 已验收（离线） / 已验收（真实业务）。
B1基线174项，B2后199项，B3后255项，B4后330项，B5后347项，S4后372项，S5后378项离线测试通过；S6新增9项运维/评测离线测试；改稿链+追问入口+专职评测Agent+人工确认+链内硬约束闸门共411随批末全量回归确认（已知 test_workbench_s5 安全用例在高负载下偶发连接超时，单文件重跑6/6通过）；20个业务案例已各执行真实运行1次（2026-09-09），repeats=3的60次扩批与10个故障案例两轮未执行；人工权威评分（human_confirmed）未执行。

## 完整执行顺序

| 顺序 | 范围 | 交付 | 当前状态 |
|---|---|---|---|
| B1 / S0及部分S1 | 案例、模式边界、配置诊断 | 20+10案例、真实与Mock不混报 | 已验收（离线）；真实服务未验收 |
| B2 / S1 | 统一入口、根任务账本和限制 | Web/CLI/评测同一路径，辅助调用计费可查 | 已验收（离线），见B2_DELIVERY.md |
| B3 / S2 | 本地文本导入、来源和产物 | 全文、哈希、定位、去重、路径边界 | 已验收（离线），见B3_DELIVERY.md；URL/搜索属B4 |
| B4 / S2 | 网页与搜索 | 实际抓取正文、来源状态、网络安全边界 | 已验收（离线），见B4_DELIVERY.md；搜索服务需实施时选定 |
| B5 / S3 | 证据→素材→提纲→初稿→审校 | 带可核查引用的报告、草稿等级与有限修订 | 已验收（离线），见B5_DELIVERY.md；真实模型业务验收待S6 |
| S4 | 会话、取消、恢复、审批、幂等 | 重启后可继续，版本和操作状态持久化 | 部分完成，见S4_DELIVERY.md；核心库与链恢复已验收（离线），执行器/Web/Agent-loop接线随S5 |
| S5 | 完整工作界面 | 资料、正文、引用、改稿、版本、导出 | 部分完成，见S5_DELIVERY.md：研究链接入Web（表单/队列/进度/停止/恢复/证据对照/版本/导出/安全）已验收（离线）；追问改稿已接通（见S5-04）；审批页、大日志分页与浏览器人工验收未做 |
| S6 | 安装、真实评测和个人试用 | 60次业务运行、20次故障试验、7天试用 | 部分完成，见S6_DELIVERY.md：评测运行器/运维工具/评分表/模板已验收（离线）；真实执行需 Key 与预算、7天人力试用 |
| S7 | 按证据优化 | 有界并行、检索优化、模型路由、可选格式 | 可选，不能阻塞首版 |

试用版A=S1+本地资料+最小S3；试用版B增加联网与完整引用；候选版=S4+S5；日常1.0须S6达标。
不预设日期，不以测试数量替代业务通过率。

## 逐项检查

| 编号 | 工作 | 状态 | 证据/未完成点 |
|---|---|---|---|
| S0-01 | 准备 20 个业务任务：8 个资料整理、8 个联网/混合研究、4 个改稿；测试材料使用公开或合成数据。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-02 | 标注必需章节、关键事实、已知冲突、允许使用的来源和不能补充的内容。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-03 | 准备 10 个故障样例：无效配置、断网、空正文、冲突、预算不足、取消、重启、重复提交、路径越界、资料内指令注入。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-04 | 保存当前 148 项测试基线；按正确行为更新断言，不为保留数字而保留错误语义。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-05 | 明确 Mock/真实评测模式，修复 --no-skip 仍使用 Mock 的问题；真实模式禁止静默回退。 | 已验收（离线） | B1案例/模式回归，见验收基线与B1_BASELINE.json |
| S0-06 | 检查版本管理基线及敏感文件，建立可回退版本；不能无差别提交所有 untracked 文件。 | 已验收 | 首次基线提交 b821308 + c21afbc（274文件，工作树干净）；审计排除 .env/.venv/workspaces/.tmp/node_modules/构建产物；legacy/config.ini（真实Key）忽略不提交；提交内无密钥（grep验证）；仓库级身份不写全局 |
| S1-01 | 建立统一研究任务请求与应用入口，供 Web、CLI、评测共用。 | 已验收（离线） | B2：ResearchApplication供Web/CLI/Agent Benchmark共用；资料业务在B3接入 |
| S1-02 | 显式区分 mock/real；真实模式缺配置、认证失败或模型不可用时明确失败，不能回退成假成功。 | 部分完成 | B1已有局部实现；剩余项须按完整要求验收 |
| S1-03 | 定义配置优先级：任务显式选择 > 项目模型档案 > 项目基础配置。记录实际 provider/model/能力/价格表版本，禁止档案悄悄覆盖选择。 | 部分完成 | B1已有局部实现；剩余项须按完整要求验收 |
| S1-04 | 先支持一个用户现有兼容配置，验证普通输出与工具调用。能力不单靠模型名字推断。 | 已验收 | deepseek-v4-flash 真实验证（2026-09-09）：通用任务工具往返 b01 passed；业务链 o01/o02/r01/v01 四类别 accepted；模型能力经由真实运行与记账确认 |
| S1-05 | 统一 LLM 调用入口，记录 root_job_id、角色、用途、模型、耗时、用量。覆盖 Planner、Replanner、Writer、Reviewer、Rerank、摘要等所有调用。 | 已验收（离线） | B2网关覆盖现有全部src调用点；线程与角色关联测试通过，旧独立演示不自动创建根任务 |
| S1-06 | 区分超时、连接、限流、认证、上下文超限等错误；只对合适的瞬时错误有限重试，记录重试与未知用量。 | 部分完成 | 关闭SDK隐藏重试，失败计未知用量并停止；细分错误/显式重试待实施 |
| S1-07 | 设置根任务总调用次数、输出 Token、估算费用、运行时间的初始限制，辅助调用和重试也计入。 | 已验收（离线） | B2初始四类限制通过；时间为边界检查，费用是估值，不是硬截止/账单封顶 |
| S1-08 | 页面显示实际模式、模型和可用工具；支持取消排队任务，运行中先显示取消请求，S4 完善执行边界。 | 部分完成 | 模式、模型、限额和用量页面已接；受控队列和取消仍待实施 |
| S1-09 | 检查配置 repr、异常链、HTTP 头、工具参数和导出中的敏感信息。保存必要脱敏诊断，不向前端返回真实密钥。 | 部分完成 | B1已有局部实现；剩余项须按完整要求验收 |
| S2-01 | 实现文本/TXT/Markdown 导入、全文保存和来源登记，区分成功、空内容、不支持、部分提取。 | 已验收（离线） | B3：ok/partial/duplicate/empty/unsupported/too_large/read_failed 全分类登记；原文只读；GB18030 兼容 |
| S2-02 | 实现 URL 获取与正文提取，限制连接/读取耗时、重定向、下载大小和内容类型；搜索摘要不能冒充已读正文。 | 已验收（离线） | B4：http/https抓取+正文提取（fetch_url/extract_document）；超时/10MB下载上限/类型白名单/重定向上限；正文与摘要分离。真实公网服务与复杂页面质量待S6 |
| S2-03 | 接一个配置明确的搜索服务，保存查询、排名、URL、标题与摘要；未配置时禁用搜索，保留本地/指定链接模式。 | 部分完成 | B4：未配置即禁用（SearchNotConfigured可操作提示、不假搜）；provider接入点与参数就绪；真实服务商待用户选定并按官方接口核验 |
| S2-04 | 实施时核查搜索服务官方接口、费用与限制；付费搜索也计入任务账本，无法估算时显式标记。 | 部分完成 | B4：SearchRecord 预留记账字段，未知成本显式 None 不记零；官方接口与费用核验待接入服务商时执行 |
| S2-05 | 为来源生成 id、获取时间和内容哈希，保存原始/最终 URL；发布日期未知就标 unknown，不把抓取时间冒充发布日期。 | 已验收（离线） | B3本地+B4网页：source_id/获取时间/sha256/规范化哈希；original_address=原始URL、final_url=重定向后URL；published_date=unknown 不冒充 |
| S2-06 | 按标题与段落切分，保留原文定位；去重相同内容，转载不当成多份独立证据。 | 已验收（离线） | B3本地+B4网页：提取正文带标题/段落，存meta字符偏移定位；同哈希/空白折叠同文判重（跨本地与网页），duplicate_of 指向原来源；转载判定以全文实质相同为限（近似转载/改写待真实样本评估） |
| S2-07 | 受控保存/读取完整产物，模型通过 artifact_id 或相对路径访问，不让报告只藏在日志里。 | 部分完成 | B3完成存储层：ArtifactStore版本/哈希/防覆盖，artifact_id与受控相对路径读取、孤儿与篡改拒绝；模型工具接通在B5 |
| S2-08 | 限定导入与输出范围，校验规范化路径、符号链接/Windows junction，禁止越界及覆盖原始资料。 | 已验收（离线） | B3：canonical+最长已存在前缀解析，junction/符号链接与不存在尾部逃逸均拒；索引与写路径全过resolve_under；原始资料永不回写。URL下载路径随B4复验 |
| S2-09 | 资料 URL 仅允许预期协议；拦截私网、回环、链路本地等地址，重定向重新校验，处理 DNS 解析与连接间地址变化。可信本地模型 endpoint 使用独立策略。 | 已验收（离线） | B4：仅http/https、禁userinfo；解析集整体白名单（私网/回环/链路本地/组播/保留/文档网段、IPv4-mapped按v4）；每跳重定向重新parse+resolve并直连解析出的IP；可信场景经 allowed_hosts 独立放行。真实DNS rebinding攻击样本/代理场景待S6 |
| S2-10 | 网页/文档作为不可信资料，不能改变系统指令、工具权限或授权外部访问；资料内的读取密钥等指令不得执行。 | 已验收（离线） | B4：网页文本无执行路径、原样入来源；测试固化：不改请求快照/系统指令、工具注册表不变、任务目录外无新文件。B5成稿链把资料喂给模型后再做真实语义复验 |
| S2-11 | 文本型 PDF 单独接入一个必要解析依赖，限制时间与大小并保留页码；扫描件明确提示需要 OCR。 | 待实施 | — |
| S3-01 | 实现资料整理、研究写作两条业务路径；固定阶段先通过，不由 LLM 任意生成执行架构。 | 部分完成 | B5：固定链已实现（证据→素材→提纲→初稿→双层审校→≤2轮修订），整理/写作由任务目标经同一链引导；真实业务案例验收待S6 |
| S3-02 | 修复 writer 丢失前序材料；交接 artifact_id、证据 id 和必要摘要，按需读取完整材料，列表摘要不能作为唯一业务输入。 | 部分完成 | B5：证据id/artifact_id显式交接、修订回带上一稿；引文带完整摘录与定位；模型"按需读取完整材料"的读取工具未开（当前按预算给引文+证据摘录），接入随后续批次 |
| S3-03 | Worker 明确接收前置产物；真正应用角色模型、技能、工具和权限，并与任务允许范围取交集。 | 部分完成 | B5：链上角色化（evidence/material/writer/reviewer 记账与提示）并显式传前置产物；研究链暂不开放工具（最小权限），多Worker编排沿用旧策略组件 |
| S3-04 | 证据提取输出来源、摘录、定位和支持的事实；程序先检查来源/位置存在，再由模型检查语义。 | 已验收（离线） | B5：quote逐字存在校验+段落定位后才入库，未定位即丢弃记error；evidence.json 权威存储 |
| S3-05 | 素材包按主题组织事实、证据、重复项、冲突和待补问题；不能由模型无依据地消除矛盾。 | 已验收（离线） | B5：冲突status强制open（模型"解决"被拒并记issue）；重复来源由程序登记补齐 |
| S3-06 | 提纲与正文区分事实、推断、未知；正文引用稳定证据 id，渲染为可点击的用户引用。 | 部分完成 | B5：提纲 required_evidence + 正文〔事实/推断/未知〕标注与 [E-xxx] 引用标记完成；"渲染为可点击引用"随S5工作台 |
| S3-07 | 每次调用前组装上下文，必需资料优先，按模型窗口控制长度；压缩仍保留原文引用，工具请求/响应成对保留。 | 部分完成 | B5：单来源40k/素材20k字符预算与显式截断标注、证据索引带id+摘录；"必需资料优先排序"与窗口自适应待真实模型样本后调 |
| S3-08 | 首版技能按业务模式明确配置；不依赖复杂自动召回决定权限，也不把全部技能塞进提示词。 | 部分完成 | B5：阶段固定提示词即"按业务模式配置"，research链不装技能/工具；skills 自动路由未接入（保持最小权限） |
| S3-09 | 双层审校：程序检查引用存在、格式、章节；模型检查支持关系、遗漏与矛盾。模型自评不能单独证明事实正确。 | 已验收（离线） | B5：程序层（引用/覆盖/章节/标注）+模型层（support/missing/conflict/style）；模型accepted 含error自动改判 |
| S3-10 | 建议最多修改 2 轮，每轮保存问题、新稿和变更。预算不足或仍有关键缺口时交付“待完善草稿”，不能显示验收成功。 | 已验收（离线） | B5：≤2轮修订，问题清单 review.vN、新稿 report.v(N+1) 版本化保存；预算停止/轮次耗尽交付draft |
| S3-11 | 执行结束与业务成功分开；最大轮数、引用失败和材料缺失不一律显示成功。 | 已验收（离线） | B5：accepted/draft/failed 分级映射 job 终态 completed/partial/cancelled/failed；final_text 保留草稿全文 |
| S3-12 | 保存每阶段产物与计划快照，为 S4 提供稳定恢复边界，暂不承诺任意图节点恢复。 | 已验收（离线） | B5：阶段产物全落 ArtifactStore，pipeline.json 阶段快照；真实恢复流程 S4 接通 |
| S3-13 | 给计划增加完整校验：重复 id、未知依赖、循环、空计划、失败传播；重规划后重验，受影响旧产物标记失效。 | 部分完成 | B5：链内契约校验（引用/证据id/空章节/失败传播）；链为固定顺序不生成任意图；DAG级校验在 planning 模块既有测试覆盖 |
| S4-01 | 用标准库 SQLite 保存任务/会话/阶段/审批/操作账本，提供 schema_version、迁移和备份。 | 已验收（离线） | src/harness/state/db.py：jobs/sessions/approvals/operations/meta；schema_version=1，高版本拒绝写入；在线备份+verify；损坏显式StateDbError不伪装空库 |
| S4-02 | 定义 queued、running、waiting_human、cancel_requested、completed、partial、failed、cancelled、interrupted，与底层 Run 状态显式映射。 | 已验收（离线） | states.py：9状态词+合法迁移表+底层Run映射+终态不可回写 |
| S4-03 | 有界队列、持久化领取/租约、启动扫描；禁止两个执行者同时恢复同一任务。 | 部分完成 | 队列组件已验收：原子领取、心跳、过期回收、startup_scan→interrupted、并发唯一赢家测试；常驻执行器与Web提交接线随S5 |
| S4-04 | 产物保存后才提交阶段成功；对账“产物已写、状态未提交”等崩溃窗口，避免盲目全量重跑。 | 已验收（离线） | runner顺序=产物→检查点→stage_hook外部状态；已有检查点拒绝无意识重跑；resume按文件恢复；操作账本 unknown 显式标记 |
| S4-05 | 加载本会话的目标、有效材料、当前版本及必要历史完成续写，默认不串用其他会话资料。 | 部分完成 | SessionStore+材料快照+跨会话隔离测试通过；链续写加载与版本恢复的完整接线随S5会话页 |
| S4-06 | 实现分阶段取消，停止新调用；已发送请求不能保证撤回，区分“正在停止”和“已停止”。 | 部分完成 | 队列两段取消+research链阶段边界收敛已验收（requested→cancelled/partial）；通用Agent循环取消门随S5执行器 |
| S4-07 | 可控本地解析/计算放在有界子进程中，超时可终止；外部操作依赖接口幂等能力，不宣称可安全强杀任意线程。 | 已验收（离线） | control/subprocess_guard.py：超时真实kill、输出截断；用途由接入方按需决定 |
| S4-08 | 操作账本使用 pending/running/succeeded/failed/unknown；幂等键绑定业务动作及版本。已知成功才回放，unknown 必须核实。 | 已验收（离线） | state/ops.py：op_key=job:action:version:params_hash；succeeded才回放；unknown拒绝自动重放 |
| S4-09 | 审批持久化，绑定动作、参数哈希、范围、版本和到期时间；参数改变、重规划和取消使旧审批失效，不只凭审批者字符串放行。 | 部分完成 | state/approvals.py 全部绑定与失效语义已验收；工具层审批接线（把内存 pending 换成库）随S5 |
| S4-10 | 完善根预算：各阶段、子 Run、辅助调用、重试与搜索统一计入；并行前预留、结束后结算；未知成本不能记为零。 | 已验收（离线） | B2网关+链purpose+子run/线程共享测试；未知成本显式None/阻止新调用；并行分支沿用串行化决策（未放开并行则无预留需求） |
| S4-11 | 重复/无进展检测依据结构化调用与产物变化，不能靠回答中的关键词决定是否成功。 | 部分完成 | control/progress.py 按(llm调用数,产物哈希)判定已验收；执行器看门狗接线随S5 |
| S4-12 | 先按业务阶段恢复；确需节点级恢复再接官方持久化 Checkpointer，避免出现两个独立调度系统。 | 已验收（离线） | 阶段检查点+resume_research_job+CLI --resume-job；真实子进程os._exit崩溃→续跑完成且不重复抽取；节点级官方checkpointer未接（决策：阶段边界足够） |
| S4-13 | 旧运行可读取；迁移失败明确提示，不以忽略解析错误、返回空列表伪装成没有历史。 | 已验收（离线） | 旧run/plan.json 布局不变（B2/B3全量回归证明）；库损坏/高版本/快照损坏全部显式报错 |
| S5-01 | 表单提供目标、资料、模式、读者、长度、模型、预算；高级参数折叠，常用默认值清楚。 | 部分完成 | 目标/资料(文件/链接/粘贴)/模式/流程/预算/次数Token秒数在表单项；读者与长度（成稿风格）与高级参数折叠未做 |
| S5-02 | 任务列表、进度、排队/停止/恢复、资料列表、最终报告成为基本视图；日志放详情。 | 已验收（离线） | 研究任务经SQLite队列（submit→claim→run→release）；㉑卡片任务列表/阶段进度/停止/恢复；agent run 走原Dashboard；日志/分级在详情区 |
| S5-03 | 正文与来源可对照；点引用能看到原文片段和位置，不只是网站首页。 | 已验收（离线） | 报告按[E-编号]分词渲染成按钮，点击展示 evidence 摘录/定位/来源；来源全文仍可点开 |
| S5-04 | 支持追问改稿、版本列表和主要变更，修订不覆盖旧版本。 | 部分完成 | 版本列表/查看/导出、单次改稿（base_draft+no_change 守卫）与追问改稿（CLI --revise-job / Web revise 端点，谱系 revises_job，旧版永不覆盖）已做；"主要变更"自动说明与会话表记忆随试用反馈 |
| S5-05 | Markdown/HTML 下载只允许任务内产物；预览与导出都处理脚本、事件属性、危险 URL 和原始 HTML。 | 已验收（离线） | 导出仅登记产物（attachment+nosniff）；预览/渲染全走 textContent/DOM 不执行；HTML富文本预览未做（text/plain pre） |
| S5-06 | 审批页说明动作、参数、范围和失效原因；已授权的常规资料读取不反复弹窗。 | 部分完成 | HITL 面板展示动作/参数；持久化审批库与失效语义已验收（S4）；失效原因展示与常规读取授权记忆页面未做 |
| S5-07 | 错误能指导操作，例如“PDF 无可提取文本”“搜索未配置”“模型不支持工具”，不只显示异常类型。 | 已验收（离线） | 导入分类/搜索未配置/链结果分级消息贯穿 HTTP400/页面；错误同时保留类型+message |
| S5-08 | 先完善轮询游标、断线重连、部分日志行、大日志分页；只有体验需要时再加 SSE/Token 流。 | 部分完成 | after 游标/断线重连（showJob 循环容错）/增量事件已有；大日志分页未做、SSE 未加（符合"必要再加"） |
| S5-09 | 写接口校验 Host/Origin、请求类型/大小和会话防护，默认只绑定回环地址。 | 已验收（离线） | Host回环白名单403/请求体≤1MB 413/仅 application/json 415；服务默认绑定127.0.0.1；会话防护沿用 approval_id/job_id 绑定 |
| S5-10 | 清晰呈现无 Key、无搜索、预算不足和部分完成；页面只暴露实际可用能力。 | 部分完成 | mode/config ready 明确、导入错误与预算/取消分级消息呈现；搜索配置状态入口与"页面只暴露可用能力"进一步收口未做 |
| S6-01 | 评测复用业务入口，不能另写更简单的测试流程；报告记录代码/数据集版本、实际模型、工具和配置快照。 | 已验收（离线） | eval/business_eval.py 全走 ResearchApplication(flow=research)；报告含 dataset version/代码版本/Python与包版本/模型与工具/脱敏配置快照 |
| S6-02 | 固定资料与 HTTP 样本用于离线回归，实际服务用于联网冒烟；两类结果分别报告。 | 部分完成 | 固定合成资料离线回归已通（stub 2/2、Mock 诚实失败）；实际服务联网冒烟脚本未执行（待Key与预算） |
| S6-03 | 真实评测显式启用并设置批次限额；没有配置/额度就标未执行，不拿 Mock 成绩顶替。 | 已验收（离线） | --mode real 必须 --max-cost；缺Key整批 not_executed 且零模型请求（测试验证）；探针不发请求 |
| S6-04 | 20 个业务任务每个重复 3 次，共 60 次，保存所有失败样本；10 个故障任务至少执行两轮。 | 部分完成 | 运行器支持 repeats/fault_rounds、失败样本整目录保存；真实 20 任务已完成两轮各 1 次：第一轮 20/20 链内 accepted（约 $1.29），闸门后第二轮 20/20 执行完成、链内 accepted 14/draft 6/failed 0（约 $1.20，`eval/reports/gate_real_*`）；repeats=3 的 60 次（估算 ≈$5）与故障全量两轮待执行 |
| S6-05 | 明确人工评分表，模型可以辅助找问题，但不能自评分后自动盖章。 | 部分完成 | eval/human_scores.py（sheet/ingest 人工确认覆盖，human_confirmed=true）+ eval/grader.py 专职评测 Agent（独立模型 GRADER_MODEL_NAME/--grader-model/--grade，程序交叉核验与规则重算，human_confirmed=false 不自动盖章）；第一轮 16 例独立打分=1 accept（均值 3.88/2.94/3.69/3.62），暴露"链内 accepted≠业务达标"（o08 缺必需章节仅236字）；据此加链内硬约束闸门（必需章节/禁语/关键事实 → 提纲补入+程序层复验，缺章节/禁语为阻塞 error，关键事实 warn；CLI/Web/评测接线 + chain_hard_checks 读数）；闸门后第二轮 20 例独立打分=**10 accept/5 draft/5 fail，均值 3.85/4.75/3.90/4.65**，必需章节命中 20/20 例满分，链内 accepted 的独立通过率由 1/16≈6% 升至 7/14=50%；20 份人工评分表已生成待填写（human_confirmed 未执行） |
| S6-06 | 记录全任务与阶段耗时、已知/估算费用、失败原因、人工改稿时间；与相同任务的单 Agent 或手工基线比较。 | 部分完成 | 逐次记录耗时/费用/未知用量/失败原因/终止原因/机器检查；改稿分钟入表；单Agent/手工基线对比未做 |
| S6-07 | 锁定已验证依赖和 Python 版本，修正安装/包发现配置；全新虚拟环境验证安装、启动、测试和样例。 | 部分完成 | requirements.lock.txt（Python 3.14.7 + 五依赖精确版本）+ python -m src.ops.verify（导入/CLI样例/诊断/健康）；真正全新venv执行留待人工 |
| S6-08 | 提供项目内启动脚本、健康检查、配置诊断、日志位置、备份恢复说明，不动全局环境和 DSH。 | 已验收（离线） | src/ops/{health,backup,verify}.py + 启动/诊断命令文档；备份=sqlite在线备份+jobs/sessions复制+manifest（roundtrip测试）；恢复说明防覆盖运行区 |
| S6-09 | 必要的浏览器回归工具作为开发依赖，覆盖正文、引用、审批、取消和改稿，不混入生产依赖。 | 部分完成 | docs/BROWSER_REGRESSION.md：覆盖矩阵+引入步骤（HTTP级已有等价覆盖）；工具引入待需要时，人工验收随试用 |
| S6-10 | 进行 7 天个人试用，至少记录 20 个真实使用任务；问题转成可复现用例，再判断是否发布 1.0。 | 待实施 | docs/TRIAL_LOG_TEMPLATE.md 模板与自查/判定步骤已备；执行需真实 Key 与 7 天人力 |

## 本批B2拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| P00 | 核对完整计划，建立逐项清单与追加日志 | 已完成 |
| B2-01 | 统一请求、根任务目录与应用执行入口 | 已验收（离线） |
| B2-02 | 模型调用网关、持久化调用账本、次数/输出/时间/费用限制 | 已验收（离线） |
| B2-03 | 主循环与Planner/Replanner/Judge/Rerank/摘要接网关；线程上下文传递 | 已验收（离线） |
| B2-04 | Web、CLI、评测共用应用入口；可查看根任务用量 | 已验收（离线） |
| B2-05 | 集成回归、失败路径、浏览器验证和交付记录 | 已验收（离线） |

## 本批B3拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| B3-00 | B2回归复核；修复 /job 轮询偶发 AttributeError（run.json root_job_id 写入窗口） | 已完成 |
| B3-01 | 受控路径边界：canonical/最长已存在前缀解析、junction与不存在尾部逃逸拒绝（S2-08本地） | 已验收（离线） |
| B3-02 | 来源登记与导入：文本/TXT/Markdown分类、全文/哈希/meta、标题段落定位、同文去重（S2-01/05/06本地） | 已验收（离线） |
| B3-03 | 受控产物 ArtifactStore：版本/哈希/防覆盖、artifact_id与相对路径受控读取（S2-07存储层） | 已验收（离线） |
| B3-04 | TaskRequest 携带资料字段；ResearchApplication/CLI 导入接入与导入摘要（统一入口原则） | 已验收（离线） |
| B3-05 | Web /api/jobs 资料与产物只读端点、页面⑳面板、全量回归与交付记录 | 已验收（离线） |

## 本批B4拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| B4-01 | URL策略与SSRF/私网防护：协议/格式白名单、地址网段拦截、DNS解析集整体校验（S2-09离线） | 已验收（离线） |
| B4-02 | 抓取器与正文提取：http/https直连白名单IP、重定向逐跳复检、超时/大小/类型限制；HTMLParser正文（S2-02） | 已验收（离线） |
| B4-03 | SourceStore 登记 URL 来源：kind=url、原始/最终URL、http状态、内容类型、跨来源转载去重（S2-05/06 URL侧） | 已验收（离线） |
| B4-04 | TaskRequest.urls/allow_network + 统一导入入口 import_request_sources + CLI/Web 接入（S2-09应用侧） | 已验收（离线） |
| B4-05 | 搜索占位网关（S2-03/04：未配置即禁用、账本字段、可操作提示）+ S2-10 惰性测试证据 | 已验收（离线） |
| B4-06 | 全量回归与交付记录（B4_DELIVERY.md），同步状态文档 | 已完成 |

## 本批B5拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| B5-01 | 数据契约与证据层：Evidence/MaterialPack/Outline/Review 契约、evidence.json、引用标记与稳定编号（S3-04数据面） | 已验收（离线） |
| B5-02 | 提示词模板与上下文预算：40k/20k字符上限、截断显式标注、证据索引带id+摘录（S3-07雏形） | 已验收（离线） |
| B5-03 | 证据提取与素材包阶段：quote逐字定位、冲突强制open、重复来源程序补齐（S3-04/05） | 已验收（离线） |
| B5-04 | 提纲与初稿阶段：required_evidence校验、[E-xxx]引用与〔事实/推断/未知〕标注（S3-06） | 已验收（离线） |
| B5-05 | 双层审校与≤2轮修订：程序+模型审校、review.vN/report.v(N+1)版本化（S3-09/10） | 已验收（离线） |
| B5-06 | runner：固定阶段、pipeline.json快照、accepted/draft/failed分级、预算停止收敛（S3-11/12） | 已验收（离线） |
| B5-07 | 入口接入：TaskRequest.flow、ResearchApplication同账本跑链、CLI --flow research、Web守卫（S1-01延续） | 已验收（离线） |
| B5-08 | 全量回归与交付记录（B5_DELIVERY.md），同步状态文档 | 已完成 |

## 本批S4拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S4核心 | state/{db,states,queue,ops,approvals,sessions}.py：SQLite库/状态词与迁移表/队列租约与两段取消/操作账本/持久化审批/会话隔离（S4-01/02/03组件/06队列侧/08/09库侧/13） | 已验收（离线） |
| S4链层 | runner 检查点与 resume、should_stop 边界收敛、stage_hook 先文件后状态、崩溃窗口恢复（S4-04/06链侧/12） | 已验收（离线） |
| S4能力 | control/subprocess_guard.py（有界子进程）与 progress.py（无进展检测）（S4-07/11组件） | 已验收（离线） |
| S4入口 | resume_research_job 应用入口 + 续接账本（原调用载入、上限延续）+ CLI --resume-job（S4-10/12应用侧） | 已验收（离线） |
| S4接线 | 常驻队列执行器、Web 提交/取消/审批/会话页面、Agent循环取消门（S4-03/05/06/09/11 剩余接线） | 待实施（随S5） |

每步完成立即追加日志，至少包含：日期、对应编号、修改文件、验证命令/结果、限制与下一步。
设计与代码存在但尚未验证时只能标“实施中”，不能提前写“已验收”。

## 本批S5拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S5-01 | ResearchApplication.run 注入 job_id/progress/stage_hook/should_stop | 已验收（离线） |
| S5-02 | Web×S4接线：state.sqlite+JobQueue+worker（submit→claim→run→release 分级收敛）与㉑任务卡 | 已验收（离线） |
| S5-03 | 端点与视图：/api/jobs、progress、evidence、artifacts download、cancel、resume | 已验收（离线） |
| S5-04 | 页面交互：表单flow/任务跟踪/停止/恢复/报告分词引用/版本/导出 | 已验收（离线） |
| S5-05 | 写接口安全（Host/大小/类型）与安全渲染（DOM/textContent、attachment+nosniff） | 已验收（离线） |
| S5-06 | 全量回归、交付记录与文档同步 | 已完成 |
| S5-剩余 | 追问改稿（会话式）、审批失效原因页、读者/长度与高级折叠、大日志分页、HTML富预览、搜索能力展示收口、真实浏览器人工验收 | 待实施（部分随S6/后续批次） |

## 本批S6拆解

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S6-运行器 | eval/business_eval.py：统一入口评测、real缺Key→not_executed、repeats/fault_rounds、失败样本保存、逐次指标、版本/配置快照（S6-01/03/04/06） | 已验收（离线） |
| S6-评分表 | eval/human_scores.py 逐次人工评分表（机器检查列明示仅供参考，不自评分盖章）（S6-05） | 已验收（离线） |
| S6-运维 | src/ops/{health,backup,verify}.py + requirements.lock.txt（S6-07/08） | 已验收（离线） |
| S6-模板 | docs/TRIAL_LOG_TEMPLATE.md、docs/BROWSER_REGRESSION.md（S6-09/10） | 已完成（模板级） |
| S6-真实执行 | 真实模型60次/联网冒烟/故障两轮/人工评分导入/7天试用/单Agent基线对比/全新venv安装验证 | 待实施（需Key+预算+人力） |




| S6-评测Agent | eval/grader.py 专职评测：评分表打分+程序核验+规则重算+独立账本+human_confirmed=false | 已验收（离线+真实演示） |
| S6-人工 | 人类评分/确认与覆盖入口、20案例全量评分执行 | 待实施（试用期/用户） |
| S6-硬约束闸门 | 链内任务硬约束：HardRequirements 契约 + 提纲补入 + 程序层复验（必需章节/禁语 error、关键事实 warn）+ CLI/Web 参数 + 入口/续跑/改稿透传 + 评测接线与 chain_hard_checks 读数 | 已验收（离线+真实批次）：20/20 例必需章节全命中；链内 accepted 的独立通过率 6%→50% |

## S8 拆解：研究主题自适应多智能体（2026-09-10 立项，产品主线）

设计见 `docs/DYNAMIC_ORCHESTRATION_PLAN.md`。决策：定位产品主线；由**专用调度智能体**读题输出结构化执行方案；子智能体用于并行拆子题、角色分工、并允许嵌套（需深度/数量/去重/预算递减护栏）；固定流程降级为候选与保底模式。**交互默认全自动（2026-09-10 用户确认）**：使用者只关心好不好用与结果，不关心中间过程——一次输入直接执行，交付时告知所选方式/花费/等级，过程记录落盘备查；「先看计划」为默认关的可选开关，仅超预算阈值或关键条件不明才停下确认。

| 步骤 | 具体产物 | 状态 |
|---|---|---|
| S8-00 | 评测纠偏与判据前置（= 下表 S8-A/B/C + 选型标注集 ≤10 例人工确认冻结）；三指标分离（执行完成率/预期行为符合率/成品质量达标率）；旧"开卷"结果保留并标记不合并；S8-05 的硬前置 | 开发完成（离线）：S8-A/B/C 已落地，20 例旧批次已按新口径重算（`eval/reports/rescore_gate_batch.*`）；标注集待人工冻结 |
| S8-01 | 最小执行方案契约 + 调度智能体（**首版只在 fixed/fanout 选型**，候选按实际能力过滤）+ 基础护栏同批落地（次数/数量/权限/预算/取消，执行器前置）+ 钱闸默认值草案 | 开发完成（离线：契约/调度/护栏 13 项测试；待真实批次验收） |
| S8-02 | 执行器接线（fixed/fanout 闭环）：方案 → 研究；子智能体产出接入来源/证据/产物；成稿与审核通道不变；失败重试一次记新尝试、不静默跳过 | 开发完成（离线：注入假应用验证接线；真实链路待冒烟） |
| S8-03 | 预算分配与并行前置：额度公式（根剩余 − 在途预留 − 成稿预留，Σ 子任务+在途+预留 ≤ 根剩余）落程序；根账本锁串行如实标注（本轮串行执行，不宣称并行加速） | 开发完成（离线：分配公式有测试；锁改造评估待 S8-05 前定论） |
| S8-04 | 入口与展示：CLI 执行方式（默认 auto）+ --plan-only + 方式告知 + 过程记录落盘 orchestration.json | 部分完成（CLI 已接；Web 计划面板/子智能体树待做，页面级验收未做） |
| S8-05 | 真实对比评测：fixed vs auto（fixed/fanout）（先 5～6 案例只做趋势判断，结论性门槛在 20 案例复验），质量/成本/时延三口径；基线一律用 S8-00 纠偏后口径；auto 臂成本含调度调用并单列 | 待实施（需预算；前置 S8-00 + S8-04；对比用数据集资料，不依赖真实搜索） |
| S8-06 | 按证据定默认模式（无收益则如实记录并保持 fixed）；manager_worker/debate/dynamic_team 与嵌套派生此后逐个走同款小批对比，验收一个开放一个 | 待实施 |
| 搜索接入 | **模拟先行（2026-09-10 用户决定）**：网关注册显式 mock provider（仅离线可选，真实模式明确拒绝 MockSearchInRealMode），确定性结果逐条带标记、SearchRecord 记账含 mock 位；真实服务商推迟到主题研究试用需要时（免费额度起步：Google CSE 100 次/天、Tavily 1000 次/月免绑卡；中文主力再评估百度千帆/博查） | Mock 已完成（离线 5 项测试）；搜索→读原文→证据的链路接线待做；真实接入前置：用户届时选定服务商 |

**同批立项的纠偏项（来自 2026-09-10 复核；2026-09-10 批量开发落地）**：

| 纠偏 | 内容 | 状态 |
|---|---|---|
| S8-A | 评测口径回归文档规定：按案例预期（成品/草稿/无法完成）算一致率，partial 单列，引用可定位率/伪造来源/未授权分别统计（此前只用评审智能体 accept 率，口径不合规） | 已完成（离线）：`eval/rescore.py` + 20 例旧批次重算读数（执行完成 20/20；预期符合 12/20；质量 7/14；r07"预期无法完成却交成品"等失配已显式暴露） |
| S8-B | 关闭"开卷"：评测时不再把数据集的关键事实/禁止断言注入写作提示词，仅保留显式对照开关 | 已完成（离线）：默认闭卷 + `--open-book` 对照 + meta 自描述标记 |
| S8-C | 禁止断言改语义判定：程序层只提示疑似命中，判定交评审/人工（文档明确"不能只靠关键词命中"） | 已完成（离线）：链内禁语命中降为 warn 疑似提示，不阻塞验收，读数照记 |

